apiVersion: apps/v1
kind: Deployment
metadata:
  name: app2
  namespace: app2
  labels:
    app: app2
spec:
  selector:
    matchLabels:
      app: app2
      tier: frontend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: app2
        tier: frontend
    spec:
      containers:
      - image: wordpress:4.8-apache
        name: wordpress
        env:
        - name: WORDPRESS_DB_HOST
          value: db.db
        - name: WORDPRESS_DB_USER
          value: app2
        - name: WORDPRESS_DB_NAME
          value: db_app2
        - name: WORDPRESS_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-pass
              key: app2
        ports:
        - containerPort: 80
          name: app2
        volumeMounts:
        - name: wordpress-persistent-storage
          mountPath: /var/www/html
      volumes:
      - name: wordpress-persistent-storage
        persistentVolumeClaim:
          claimName: app2-pvc-claim
